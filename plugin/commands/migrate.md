---
description: "Convert one LLM classifier to a decisionsmith harness in shadow mode"
---
Use the decisionsmith-migrate skill on $ARGUMENTS (file:line). Keep the LLM as teacher, add Laya in shadow mode, add a FakeEngine test, show the diff before applying.
