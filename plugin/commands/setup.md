---
description: "Connect and check decisionsmith engines"
---
Use the decisionsmith-setup skill. Check installs and keys with `decisionsmith doctor --json`, then ask which engines to test before running `--engines`.
